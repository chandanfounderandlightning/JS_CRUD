import styled from 'styled-components';
import { Banner } from '../../../../assets/images';

export default styled.div`
  width: 50%;
  display: flex;
  background: url(${Banner}) no-repeat top right / cover;
  height: 673px;
  position: absolute;
  right: 0;
  top: 0;
`;
